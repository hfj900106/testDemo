package com.hzed.easyget.infrastructure.repository;

import org.springframework.stereotype.Repository;

/**
 * @author wuchengwu
 * @since 2018/5/21
 */
@Repository
public class SystemOptionRepository {

   /* @Autowired
    private SystemOptionMapper systemOptionMapper;

    public SystemOption findByKey(String s) {
        SystemOptionExample example = new SystemOptionExample();
        example.createCriteria().andKeyEqualTo(key);

        List<SystemOption> systemOptions = systemOptionMapper.selectByExample(example);
        return ObjUtil.isEmpty(systemOptions) ? null : systemOptions.get(0);
    }*/
}